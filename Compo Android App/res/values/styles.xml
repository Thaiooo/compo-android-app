<resources>

    <style name="AppBaseTheme" parent="android:Theme.Light"></style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme"></style>
    
     <!-- Main Menu theme. -->
    <style name="MainMenuTheme" parent="@android:style/Theme.NoTitleBar"></style>

</resources>